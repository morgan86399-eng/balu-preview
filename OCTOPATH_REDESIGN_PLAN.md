# 《八路列傳》整包對齊《歧路旅人》UI／介面／遊戲性 — 改造規格

使用者已確認「OK，動手改」。目標：把現有 Vite + React 網頁原型，整包改成更接近《歧路旅人》（Octopath Traveler）的可玩體驗，不是局部換皮。

## 專案現況

- 技術：Vite + React + TypeScript + GSAP
- 已有：八人開局、路徑行動、蓄力 0–3、削盾／崩解、新野城鎮格移動、戰鬥、存檔、金線暗底 UI
- 設計覆寫：`design-system/default/pages/game.md` 優先於 MASTER（禁 Inter／企業綠；用 Noto Serif TC／Sans TC；主色金 `#e4c56a`）
- 源碼壓縮包：`uploads/balu-src-latest.tgz`（解壓後即專案根；無 node_modules／dist／.git）

## 成功標準（Done when）

1. `npm test` 全過；`npx tsc --noEmit` 過；`npm run build` 成功。
2. 標題／選角／城鎮／對話／選單／戰鬥六大畫面，視覺與資訊層級明顯更接近歧路旅人（見下方「必須改」），且仍是三國題材（不可直接抄 OT 商標／資產）。
3. 手機 320／375／390／412／768：無整頁橫向捲動；主按鈕 ≥ 44×44；`100dvh` + safe-area。
4. `prefers-reduced-motion: reduce` 時 GSAP／CSS 動畫關閉或 duration 0。
5. 玩法迴路完整可通關第一章：選角 → 新野路徑行動探聽 → 城門削盾戰 → 官道 → 北營渠帥；蓄力／削盾／崩解手感清楚。
6. README 更新本機啟動與操作說明（路徑可寫相對專案根）。

## 必須改（優先，整包）

### A. 戰鬥 UI（最像歧路旅人的一塊）

- 畫面上方：**出手順序列**（頭像條，當前回合高亮）。
- 敵人：**盾牌點數**改成可辨識的盾圖示列（不是難辨的小點）；破盾後明確「崩解」狀態與弱點加倍感。
- 我方：**蓄力（Boost）**改成 0–3 顆可點的圓珠／燈，選指令後再選蓄力層數；蓄力消耗與多段攻擊回饋清楚。
- 指令板：底部橫向指令列（攻擊／戰技／防禦／物件），金框暗底、選中高亮；戰技／物件進子選單再回上一層。
- 傷害數字、弱點命中、崩解 flash 要短促可讀（GSAP 只動 transform／opacity）。
- 弱點揭示後用武器圖示列，不要只靠文字擠在一起。

### B. 城鎮／探索介面

- 角色與 NPC 在場景中的站位、名稱牌、靠近才顯示路徑行動（探聽／挑戰／威嚇／偷竊等既有系統），遠距不堆字。
- HUD：左上地名＋時段；右上迷你地圖精簡；目標條更像「當前篇章目標」橫幅，不要長教學文牆。
- 路徑行動確認：彈出角色專屬路徑行動面板（名稱、成功率／條件、結果預覽一句），再執行。
- 觸控：左下方向、右下情境鍵（交談／路徑行動／列傳）保持 ≥44px，不擋角色。

### C. 標題／選角／對話／選單

- 標題：電影感暗底、書法／襯線大標、金線框 CTA；進場用 GSAP。
- 選角：八人卡保留路徑行動標籤；選中有立繪放大＋簡短列傳摘要＋「動身」確認。
- 對話框：底部寬對話箱、說話者名牌、立繪半身可選；逐句推進。
- 列傳選單：道具／狀態／存檔分頁，金框暗底，鍵盤／觸控都可用。

### D. 遊戲性對齊（邏輯層，不只皮）

- 維持並強化：弱點削盾 → 崩解窗口 → 蓄力多段爆發。
- 路徑行動依職業／英雄差異（現有 data 可擴充描述與 UI，不要砍掉八人差異）。
- 戰鬥 log 精簡為螢幕中央短訊＋歷史可選，不要蓋住立繪。
- 若現有單人戰鬥為主，至少 UI／流程要能擴成「多角色出手順序」（即使第一章常是一人開場，結構先對齊 OT）。

### E. 設計系統

- 把 `pages/game.md` 的規則寫進實際 CSS token（已有部分 `--gold` 等）；MASTER 的 Inter／綠 `#15803D` 不得出現在執行期 UI。
- 圖示用 SVG（現有 Icon.tsx），禁用 emoji 當主 UI。

## 不要做

- 不要改成別的 IP 玩法（不要變牧場／放置）。
- 不要引入大型新框架；維持 React + Vite + GSAP。
- 不要刪測試；失敗就修到過。
- 不要依賴本機絕對路徑 `/Users/weiyo/...`。
- 不要加入需要 API key 的生成圖管線；沿用 `public/art/`。

## 建議實作順序

1. 解壓 `uploads/balu-src-latest.tgz` 為專案根，`npm install`。
2. 先改 `BattleView` + `game.css` 戰鬥層（出手順序、盾、蓄力珠、指令板）。
3. 再改 `WorldView`／路徑行動面板／目標橫幅。
4. 標題／選角／DialogueBox／MenuOverlay 視覺統一。
5. 必要時微調 `combat.ts`／`pathAction.ts` 以配合 UI 流程（驗證假設，可推翻）。
6. 跑 test／tsc／build；修到綠。

## 回報給協調者

- 改了哪些檔、戰鬥／城鎮各一張行為說明。
- 測試結果（test／tsc／build）。
- 已知未做（例如第二章、音效素材）。
